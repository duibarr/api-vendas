describe('CreateCustomer', () => {
    it('should be able to create a new customer', () => {
        expect(5 + 5).toBe(10);
    });

    it('should not be able to create a new customer if the email already exists', () => {
        expect(5 + 1).toBe(10);
    });
});
